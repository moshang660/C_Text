#include <stdio.h>
#include <stdbool.h>

//int main() {
    //输出100-200之间的素数
    /*int i, j;
    bool is_prime;

    for (i = 100; i <= 200; i++) {
        is_prime = true;

        for (j = 2; j <= i / 2; j++) {
            if (i % j == 0) {
                is_prime = false;
                break;
            }
        }

        if (is_prime == true) {
            printf("%d\n", i);
        }
    }

    return 0;*/

    //九九乘法表
    /*int i, j;
    for (i = 1; i <= 9; i++) {
        for (j = 1; j <= i; j++) {
            printf("%d*%d=%d\t", j, i, j * i);
        }
        printf("\n");
    }
    return 0;*/



    //判断1000年---2000年之间的闰年
    /*int year;
    printf("请输入你要查询的年份(1000-2000)");
    scanf("%d", &year);
    if (year%400==0)
    {
        printf("%d是闰年", year);
    }
    else {
        printf("%d不是闰年", year);
    }
    return 0;*/


    /*int a = 5;
    int b = 10;
    int temp;

    printf("交换前：a=%d, b=%d\n", a, b);

    temp = a;
    a = b;
    b = temp;

    printf("交换后：a=%d, b=%d\n", a, b);

    return 0;*/

    //交换两个变量的内容
    /*int a = 6;
    int b = 9;

    printf("交换前：a=%d, b=%d\n", a, b);

    a = a + b;
    b = a - b;
    a = a - b;

    printf("交换后：a=%d, b=%d\n", a, b);

    return 0;*/
    /*int max = 0;
    int arr[] = { 10,2,40,5,33,99,56,75,26,8 };
    for (int i = 0; i < 9; i++)
    {
        if (arr[i] > arr[i+1])
        {
            max = arr[i];
        }
        else
        {
            max = arr[i + 1];
        }
    }
    printf("最大值为%d", max);*/
  /*  int max = 0;
    int arr[] = { 10,2,40,5,33,99,56,75,26,120 };
    for (int i = 0; i < 10; i++) {
        if (i == 0 || arr[i] > max) {
            max = arr[i];
        }
    }
    printf("最大值为%d", max);
    return 0;*/

    /*int count = 0;
    for (int i = 1; i <= 100; i++) {
        if (i % 2 != 0) {
            printf("%d ", i);
        }
    }*/
    


   





//}
